name: Deploy ON Push

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Testing
        run: |
            echo "Testing CI/CD pipelines"
            echo "Petch x Poom"